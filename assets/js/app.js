const add = (numOne, numTwo) => {
    const resultAdd = numOne + numTwo;
    return resultAdd;
}

const multiply = (numThree, numFour) => {
    const resultMultiply = numThree * numFour;
    return resultMultiply;
}

